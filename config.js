var config = {
	database: {
		host:	  'localhost', 	// database host
		user: 	  'root', 		// your database username
		password: 'heartbeat', 		// your database password
		port: 	  3306, 		// default MySQL port
		db: 	  'my_db' 		// your database name
	},
	server: {
		host: 'localhost',
		port: '8080'
	}
}

module.exports = config
